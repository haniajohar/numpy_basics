# NumPy, Matplotlib, and OpenCV Fundamentals

This notebook serves as an exploration and demonstration of fundamental concepts in numerical computing and image processing using popular Python libraries:

- **NumPy**: Understanding array views vs. copies, and conversions between NumPy arrays and Python lists.
- **Matplotlib**: Creating various types of plots, including line plots and stem plots, and organizing them using subplots.
- **OpenCV & Matplotlib for Image Processing**: Loading, displaying, and manipulating RGB images. This includes:
    - Separating and visualizing individual color channels (Red, Green, Blue).
    - Applying basic intensity adjustments (e.g., `red_more`, `green_less`).
    - Implementing a custom "peak softening" function to adjust image brightness.
    - Performing manual 2D convolution for edge detection using custom kernels.

## Sections:

1.  **NumPy Array Basics**: Differentiating between views and copies of NumPy arrays, and conversions to/from Python lists.
2.  **Matplotlib Plotting**: Demonstrations of simple plots and subplots for visualizing data (e.g., sine waves, discrete signals).
3.  **Image Processing with OpenCV and Matplotlib**: Loading and displaying an image, channel separation, intensity modification, and applying image filters (peak softening, edge detection) with custom kernels.
